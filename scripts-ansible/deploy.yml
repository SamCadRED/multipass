---
# Lancer le deploy des scripts
# ansible-playbook -i prod.yml deploy.yml
# Tester ansible : ansible -i prod.yml all -m ping

- name: "install docker"
  import_playbook: docker.yml

#- name: "example"
#  import_playbook: example.yml